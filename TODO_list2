Make a use of the method and then store it in that GameMaster object, -- done
main - make a button end turn -- done
Add trade button - done
make edges' midpoints buttons for roads- when clicked on --
make bot player class UI -- Still working on it -- just need to put prettier pictures

For the backend coders:
make it so when the user places a settlement it will increment the "player's victory point" by !!!!!!!one!!!!!!!.
make it so after the user places a settlement it will increment it's resource type by one according to the dice's output
and the user's settlement placement. --  done

